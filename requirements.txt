# Pinned 2026-07-12. Kordent's output is a function of data SEMANTICS.
#
# yfinance 1.x changed dividendYield from fraction to percent, corrupting
#   lynch_pass and dorsey_pass, and made yf.download return MultiIndex columns.
#   yfinance's native HTTP backend curl_cffi is now pinned — it was floating,
#   which is what a fresh uv rebuild resolved into a segfault on price download.
# pandas/numpy stepped back off the 3.0 / 2.4 bleeding edge: every pct_change
#   call passes fill_method=None explicitly (app.py, portfolio_tracker.py,
#   selector.py, stats.py), so 2.x is behaviourally identical here and lands
#   pyarrow/streamlit/curl_cffi on mature, ABI-matched wheels.

yfinance==1.5.1
curl_cffi==0.15.0
pandas==2.3.3
numpy==2.3.5
supabase==2.31.0
postgrest==2.31.0
httpx==0.28.1
google-genai==2.11.0
requests==2.34.2
pymupdf==1.28.0
protobuf>=5.27,<6
pyarrow==18.1.0

streamlit
reportlab
matplotlib
plotly
pyxirr
kaleido
